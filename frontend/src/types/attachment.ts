export type Attachment = {
  id: string;
  issueId: string;
  uploaderId: string;
  uploaderName: string;
  filename: string;
  url: string;
  mimeType: string;
  sizeBytes: number;
  createdAt: string;
};
